apiVersion: v1
kind: Service
metadata:
  name: {{ include "kronaxis-router.fullname" . }}
  labels:
    {{- include "kronaxis-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kronaxis-router.selectorLabels" . | nindent 4 }}
