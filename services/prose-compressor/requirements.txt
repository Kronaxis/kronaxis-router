# Install torch FIRST, matched to your GPU host's CUDA version, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then: pip install -r requirements.txt
llmlingua>=0.2.2
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
